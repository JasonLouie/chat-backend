export enum UserStatus {
    ONLINE = "online",
    OFFLINE = "offline",
    BUSY = "busy",
    AWAY = "away"
}

export enum UserRole {
    ADMIN = "admin",
    MODERATOR = "moderator",
    MEMBER = "member"
}

export enum MessageType {
    TEXT = "text",
    IMAGE = "image",
    VOICE = "voice",
    SYSTEM = "system"
}